/*
void setState(MessageState state) {
  messageState = state;
  notifyAllObservers();
}

void notifyAllObservers() {
  for (var observer in observers) {
    observer.update();
  }
}

void attach(MessageState observer) {
  observers.add(observer);
}
abstract class Observer {
  final MessageContext messageContext = MessageContext();
  void update();
}
*/
